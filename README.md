# Melo

A music streaming web app built with Python and Flask. Streams real audio from YouTube with no login required.

## Run locally

pip install flask yt-dlp
python app.py

Then open http://localhost:5000
